import TextChild from '@/components/TextChild';
import Layout from '@/components/common/Layout';

const Home = () => {
  return (
    <Layout>
      <TextChild />
    </Layout>
  );
};

export default Home;
